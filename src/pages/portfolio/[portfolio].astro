---
export function getStaticPaths() {
  return [{ params: { portfolio: "savante-design" } }];
}

const { portfolio } = Astro.params;

import Layout from "../../layouts/Layout.astro";
---

<Layout title={portfolio}>{portfolio}</Layout>
